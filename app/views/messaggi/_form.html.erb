<%= form_for(@messaggio) do |f| %>
  <% if @messaggio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@messaggio.errors.count, "error") %> prohibited this messaggio from being saved:</h2>

      <ul>
      <% @messaggio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :testo %><br />
    <%= f.text_area :testo %>
  </div>
  <div class="field">
    <%= f.label :allegato_id %><br />
    <%= f.number_field :allegato_id %>
  </div>
  <div class="field">
    <%= f.label :utente_id %><br />
    <%= f.number_field :utente_id %>
  </div>
  <div class="field">
    <%= f.label :conversazione_id %><br />
    <%= f.number_field :conversazione_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
