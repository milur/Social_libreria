<%= form_for(@conversazione) do |f| %>
  <% if @conversazione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conversazione.errors.count, "error") %> prohibited this conversazione from being saved:</h2>

      <ul>
      <% @conversazione.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descrizione %><br />
    <%= f.text_area :descrizione %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
